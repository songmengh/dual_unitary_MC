directory density contains the fortran code for simulating the density frofile for a dual unitary circuit with parameter alpha. Users should insert the parameters in the "input.in" file (keep nrep=1 since we only need one replica now).